<Window x:Class="MarkingMachineFeeder.Windows.PermissionConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:MarkingMachineFeeder.Viewmodel"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="750" Width="1100"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        d:DataContext="{d:DesignInstance Type=vm:PermissionConfigViewModel, IsDesignTimeCreatable=False}">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- 渐变背景画刷 - SteelBlue 风格 -->
        <LinearGradientBrush x:Key="BackgroundGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#4682B4" Offset="0"/>
            <GradientStop Color="#2F4F4F" Offset="1"/>
        </LinearGradientBrush>
        
        <!-- 现代文本框样式 -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="BorderBrush" Value="#E9ECEF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        VerticalAlignment="Center"
                                        Margin="14,0,15,0"
                                        HorizontalAlignment="Stretch"
                                        HorizontalScrollBarVisibility="Hidden"
                                        VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#4682B4"/>
                                <Setter Property="Background" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 现代按钮样式 - SteelBlue 风格 -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Height" Value="45"/>
            <Setter Property="MinWidth" Value="110"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#4682B4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="10" Padding="15,0">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#5A9FD4"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#36648B"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#B0C4DE"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Secondary button style -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Height" Value="45"/>
            <Setter Property="MinWidth" Value="110"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="#4682B4"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#4682B4"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10" Padding="15,0">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E8F4FD"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#D0E4F7"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#F5F5F5"/>
                                <Setter Property="Foreground" Value="#B0B0B0"/>
                                <Setter Property="BorderBrush" Value="#D0D0D0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 复选框样式 -->
        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="CheckBorder" Grid.Column="0"
                                    Width="18" Height="18"
                                    Background="White"
                                    BorderBrush="#4682B4"
                                    BorderThickness="2"
                                    CornerRadius="3"
                                    VerticalAlignment="Center">
                                <Path x:Name="CheckMark"
                                      Data="M2,6 L6,10 L14,2"
                                      Stroke="White"
                                      StrokeThickness="2"
                                      Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter Grid.Column="1" 
                                            Margin="8,0,0,0"
                                            VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckBorder" Property="Background" Value="#4682B4"/>
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#5A9FD4"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="CheckBorder" Property="Background" Value="#F5F5F5"/>
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#D0D0D0"/>
                                <Setter Property="Foreground" Value="#B0B0B0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 标题样式 -->
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,10,0,8"/>
            <Setter Property="Foreground" Value="#4682B4"/>
        </Style>
        
        <!-- Card style -->
        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E9ECEF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="8" Direction="270" Opacity="0.1" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- ListBox样式 -->
        <Style x:Key="ModernListBox" TargetType="ListBox">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        </Style>
        
        <!-- ListBoxItem样式 -->
        <Style x:Key="ModernListBoxItem" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#E8F4FD"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#F5F5F5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <!-- 主容器，带圆角和阴影 -->
    <Border Background="White" CornerRadius="20" Margin="10">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="0" BlurRadius="25" Opacity="0.25" Color="Black"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="120"/>  <!-- Header区域 -->
                <RowDefinition Height="*"/>    <!-- 内容区域 -->
                <RowDefinition Height="Auto"/> <!-- 底部按钮区域 -->
            </Grid.RowDefinitions>
            
            <!-- 渐变背景头部 -->
            <Border Grid.Row="0" Background="{StaticResource BackgroundGradient}" CornerRadius="20,20,0,0">
                <Grid>
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <!-- Logo或图标 -->
                        <Ellipse Width="45" Height="45" Fill="White" Opacity="0.2" Margin="0,0,0,8"/>
                        <TextBlock Text="🔒" FontSize="24" HorizontalAlignment="Center" Margin="0,-40,0,12"/>
                        
                        <!-- 标题 -->
                        <TextBlock Text="{Binding WindowTitle}" 
                                  d:Text="权限配置"
                                  FontSize="22" 
                                  FontWeight="Bold" 
                                  Foreground="White" 
                                  HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding WindowDescription}" 
                                  d:Text="配置用户角色和权限"
                                  FontSize="13" 
                                  Foreground="White" 
                                  Opacity="0.85"
                                  HorizontalAlignment="Center" 
                                  Margin="0,4,0,0"/>
                    </StackPanel>
                    
                    <!-- 关闭按钮 -->
                    <Button Content="✕" 
                            HorizontalAlignment="Right" 
                            VerticalAlignment="Top" 
                            Width="35" Height="35" 
                            Margin="15,15,15,0"
                            Background="Transparent" 
                            BorderThickness="0" 
                            Foreground="White" 
                            FontSize="18"
                            Command="{Binding CloseCommand}"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="4">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Border" Property="Background" Value="#FFFFFF30"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>
            
            <!-- 主内容区域 -->
            <Grid Grid.Row="1" Margin="20,20,20,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- 左侧：角色/用户列表 -->
                <Border Grid.Column="0" Style="{StaticResource CardBorder}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <!-- 角色列表 -->
                        <TextBlock Grid.Row="0" Text="{Binding RoleListTitle}" 
                                  d:Text="角色列表"
                                  Style="{StaticResource SectionTitle}" 
                                  Margin="20,20,20,5"/>
                        <ListBox Grid.Row="1" 
                                 ItemsSource="{Binding Roles}" 
                                 SelectedItem="{Binding SelectedRole}"
                                 Margin="15,0,15,10"
                                 Style="{StaticResource ModernListBox}"
                                 ItemContainerStyle="{StaticResource ModernListBoxItem}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="12,10" Margin="0,2" CornerRadius="8">
                                        <TextBlock Text="{Binding LocalizedRoleDisplayName}" 
                                                  FontWeight="SemiBold" 
                                                  FontSize="14"
                                                  Foreground="#495057"/>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        
                        <!-- 用户列表 -->
                        <TextBlock Grid.Row="2" Text="{Binding UserSpecialPermissionsTitle}" 
                                  d:Text="用户特殊权限"
                                  Style="{StaticResource SectionTitle}" 
                                  Margin="20,15,20,5"/>
                        <ListBox Grid.Row="3" 
                                 ItemsSource="{Binding Users}" 
                                 SelectedItem="{Binding SelectedUser}"
                                 Margin="15,0,15,20"
                                 Style="{StaticResource ModernListBox}"
                                 ItemContainerStyle="{StaticResource ModernListBoxItem}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="12,10" Margin="0,2" CornerRadius="8">
                                        <TextBlock Text="{Binding Username}" 
                                                  FontSize="14"
                                                  Foreground="#495057"/>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>
                
                <!-- 右侧：权限配置 -->
                <Border Grid.Column="2" Style="{StaticResource CardBorder}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <Grid Margin="20">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <!-- 当前选中项信息 -->
                            <Border Grid.Row="0" Background="#F8F9FA" CornerRadius="10" Padding="15" Margin="0,0,0,20">
                                <StackPanel>
                                    <TextBlock FontSize="16" FontWeight="SemiBold" 
                                              Foreground="#495057" 
                                              Text="{Binding CurrentConfigDisplayText}"
                                              d:Text="当前配置：管理员"/>
                                    <TextBlock Text="{Binding CurrentConfigDescription}" 
                                             d:Text="角色权限配置"
                                             FontSize="13" 
                                             Foreground="#9CA3AF" 
                                             Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                            
                            <!-- 权限列表 -->
                            <ItemsControl Grid.Row="1" ItemsSource="{Binding PermissionCategories}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,10" Background="#FAFAFA" 
                                               BorderBrush="#E9ECEF" 
                                               BorderThickness="1" 
                                               CornerRadius="10" 
                                               Padding="20">
                                            <Border.Effect>
                                                <DropShadowEffect BlurRadius="4" Direction="270" Opacity="0.05" ShadowDepth="1"/>
                                            </Border.Effect>
                                            <StackPanel>
                                                <!-- 分类标题 -->
                                                <TextBlock Text="{Binding LocalizedCategoryName}" 
                                                         Style="{StaticResource SectionTitle}" 
                                                         Margin="0,0,0,12"/>
                                                
                                                <!-- 该分类下的权限 -->
                                                <ItemsControl ItemsSource="{Binding Permissions}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Padding="10,8" Margin="0,3" CornerRadius="6">
                                                                <Border.Style>
                                                                    <Style TargetType="Border">
                                                                        <Setter Property="Background" Value="Transparent"/>
                                                                        <Style.Triggers>
                                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                                <Setter Property="Background" Value="White"/>
                                                                            </Trigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </Border.Style>
                                                                <Grid>
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="35"/>
                                                                        <ColumnDefinition Width="*"/>
                                                                    </Grid.ColumnDefinitions>
                                                                    
                                                                    <CheckBox Grid.Column="0" 
                                                                            IsChecked="{Binding IsGranted}"
                                                                            IsEnabled="{Binding IsEditable}"
                                                                            Style="{StaticResource ModernCheckBox}"
                                                                            VerticalAlignment="Top"
                                                                            Margin="0,3,0,0"/>
                                                                    
                                                                    <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                                                        <TextBlock Text="{Binding LocalizedDisplayName}" 
                                                                                 FontWeight="Medium" 
                                                                                 FontSize="14"
                                                                                 Foreground="#495057"/>
                                                                        <TextBlock Text="{Binding LocalizedDescription}" 
                                                                                 FontSize="12" 
                                                                                 Foreground="#9CA3AF"
                                                                                 TextWrapping="Wrap"
                                                                                 Margin="0,3,0,0"/>
                                                                    </StackPanel>
                                                                </Grid>
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </ScrollViewer>
                </Border>
            </Grid>
            
            <!-- 底部按钮区域 -->
            <Border Grid.Row="2" Background="#F8F9FA" 
                   BorderBrush="#E9ECEF" 
                   BorderThickness="0,1,0,0" 
                   CornerRadius="0,0,20,20"
                   Padding="20,15">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <Button Content="{Binding AddRoleButtonText}" 
                               d:Content="添加角色"
                               Command="{Binding AddRoleCommand}" 
                               Style="{StaticResource SecondaryButton}" 
                               Margin="0,0,5,0"/>
                        <Button Content="{Binding DeleteRoleButtonText}" 
                               d:Content="删除角色"
                               Command="{Binding DeleteRoleCommand}" 
                               Style="{StaticResource SecondaryButton}"/>
                        <Rectangle Width="1" Fill="#E9ECEF" Margin="15,8"/>
                        <Button Content="{Binding ImportConfigButtonText}" 
                               d:Content="导入配置"
                               Command="{Binding ImportConfigCommand}" 
                               Style="{StaticResource SecondaryButton}" 
                               Margin="0,0,5,0"/>
                        <Button Content="{Binding ExportConfigButtonText}" 
                               d:Content="导出配置"
                               Command="{Binding ExportConfigCommand}" 
                               Style="{StaticResource SecondaryButton}"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="{Binding ApplyButtonText}" 
                               d:Content="应用"
                               Command="{Binding ApplyCommand}" 
                               Style="{StaticResource ModernButton}" 
                               Margin="0,0,5,0"/>
                        <Button Content="{Binding SaveButtonText}" 
                               d:Content="保存"
                               Command="{Binding SaveCommand}" 
                               Style="{StaticResource ModernButton}" 
                               Margin="0,0,5,0"/>
                        <Button Content="{Binding CancelButtonText}" 
                               d:Content="取消"
                               Command="{Binding CancelCommand}" 
                               Style="{StaticResource SecondaryButton}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>