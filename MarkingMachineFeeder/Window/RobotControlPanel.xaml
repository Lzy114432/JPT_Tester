<UserControl x:Class="MarkingMachineFeeder.Windows.RobotControlPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:MarkingMachineFeeder.Viewmodel"
             mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="280"
             d:DataContext="{d:DesignInstance Type=vm:MainWindowViewModel, IsDesignTimeCreatable=False}">
    <UserControl.Resources>
        <!-- Industrial Test Button Style -->
        <Style x:Key="IndustrialTestButton" TargetType="Button">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#4682B4" Offset="0"/>
                        <GradientStop Color="#36648B" Offset="0.5"/>
                        <GradientStop Color="#27408B" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                SnapsToDevicePixels="true">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" Opacity="0.1" ShadowDepth="1" BlurRadius="3"/>
                            </Border.Effect>
                            <ContentPresenter x:Name="contentPresenter" 
                                            Focusable="False" 
                                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                            Margin="{TemplateBinding Padding}" 
                                            RecognizesAccessKey="True" 
                                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" 
                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDefaulted" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#5A5A5A" Offset="0"/>
                                            <GradientStop Color="#3D3D40" Offset="0.5"/>
                                            <GradientStop Color="#2E2E2E" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="border" Property="BorderBrush" Value="#777777"/>
                                <Setter Property="Foreground" Value="#FFFF00"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#3A3A3A" Offset="0"/>
                                            <GradientStop Color="#1D1D20" Offset="0.5"/>
                                            <GradientStop Color="#0E0E0E" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="border" Property="BorderBrush" Value="#999999"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter TargetName="border" Property="Background" Value="#F4F4F4"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#E0E0E0"/>
                                <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="#999999"/>
                                <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <!-- 机械手手动区域 -->
    <GroupBox Header="机械手手动区域" Margin="5,5,5,5" Padding="8">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Row 1: 抓料操作和放置到料仓1 -->
            <Button Grid.Row="0" Grid.Column="0"
                    Content="抓上物料到扫码区"
                    Command="{Binding GrabToScanCommand}"
                    Style="{StaticResource IndustrialTestButton}"
                    FontSize="10"
                    Height="30"
                    Margin="2"
                    ToolTip="抓取上料皮带物料到扫码区"/>

            <Button Grid.Row="0" Grid.Column="1"
                    Content="放置到料仓1"
                    Command="{Binding PlaceToBin1Command}"
                    Style="{StaticResource IndustrialTestButton}"
                    FontSize="10"
                    Height="30"
                    Margin="2"
                    ToolTip="将物料放置到料仓1"/>
            
            <!-- Row 2: 放置到料仓2和料仓3 -->
            <Button Grid.Row="1" Grid.Column="0"
                    Content="放置到料仓2"
                    Command="{Binding PlaceToBin2Command}"
                    Style="{StaticResource IndustrialTestButton}"
                    FontSize="10"
                    Height="30"
                    Margin="2"
                    ToolTip="将物料放置到料仓2"/>

            <Button Grid.Row="1" Grid.Column="1"
                    Content="放置到料仓3"
                    Command="{Binding PlaceToBin3Command}"
                    Style="{StaticResource IndustrialTestButton}"
                    FontSize="10"
                    Height="30"
                    Margin="2"
                    ToolTip="将物料放置到料仓3"/>
            
            <!-- Row 3: 从料仓取料到扫码区 -->
            <Button Grid.Row="2" Grid.Column="0"
                    Content="料仓1取料到扫码区"
                    Command="{Binding PickFromBin1Command}"
                    Style="{StaticResource IndustrialTestButton}"
                    FontSize="9"
                    Height="30"
                    Margin="2"
                    ToolTip="从料仓1取料到扫码区"/>

            <Button Grid.Row="2" Grid.Column="1"
                    Content="料仓2取料到扫码区"
                    Command="{Binding PickFromBin2Command}"
                    Style="{StaticResource IndustrialTestButton}"
                    FontSize="9"
                    Height="30"
                    Margin="2"
                    ToolTip="从料仓2取料到扫码区"/>

            <!-- Row 4: 料仓3取料和放入小车 -->
            <Button Grid.Row="3" Grid.Column="0"
                    Content="料仓3取料到扫码区"
                    Command="{Binding PickFromBin3Command}"
                    Style="{StaticResource IndustrialTestButton}"
                    FontSize="9"
                    Height="30"
                    Margin="2"
                    ToolTip="从料仓3取料到扫码区"/>

            <Button Grid.Row="3" Grid.Column="1"
                    Content="放入小车"
                    Command="{Binding PlaceToCartCommand}"
                    Style="{StaticResource IndustrialTestButton}"
                    FontSize="10"
                    Height="30"
                    Margin="2"
                    ToolTip="将物料放入小车"/>
        </Grid>
    </GroupBox>
</UserControl>